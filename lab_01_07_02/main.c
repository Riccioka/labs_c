#include <stdio.h>
#include <math.h>

int main()
{
    double x, e, f, s, d1, d2;
    if (scanf("%lf", &x) == 0 || scanf("%lf", &e) == 0 || x > 1 || x < -1)
        return 1;

    printf("%lf %lf %lf %lf", s, f, d1, d2);
    return 0;
}
